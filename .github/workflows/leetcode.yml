# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
        
      - name: Setup Python 3.9
        uses: actions/setup-python@v2
        with:
          # Semantic version range syntax or exact version of a Python version
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi:  

      # Runs a single command using the runners shell
      - name: Crawl daily challenge from leetcode.com
        id: crawl
        env:
          USERNAME: ${{ secrets.LEETCODE_USERNAME }}
          PASSWORD: ${{ secrets.LEETCODE_PASSWORD }}
        run: echo ::set-output name=FILENAME::$(echo "test-filename.json")

      # Runs a set of commands using the runners shell
      - name: Send email
        env:
          HOST: ${{ secrets.SMTP_HOST }}
          USERNAME: ${{ secrets.SMTP_USERNAME }}
          PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SENDER: ${{ secrets.EMAIL_SENDER }}
          RECEIVERS: ${{ secrets.EMAIL_RECEIVERS }}
        run: echo ${{ steps.crawl.outputs.FILENAME }}
